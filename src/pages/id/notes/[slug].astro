---
import PostView from '@components/views/PostView.astro';
import { getRoutesForSection } from '../../../lib/blog';
export async function getStaticPaths() { return getRoutesForSection('id', 'note'); }
const { entry, slug, section } = Astro.props;
---

<PostView entry={entry} slug={slug} lang="id" section={section} />
